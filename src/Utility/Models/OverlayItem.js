export class OverlayItem {
    title;
    type;
    video_url;

    constructor(title, type, video_url) {
        this.title = title;
        this.type = type;
        this.video_url = video_url;
    }
}